const SUPABASE_URL='https://zvvkfevppxjtkziyubvc.supabase.co';
const SUPABASE_KEY='sb_publishable_q3--z_x6JLXWimyKF12wmQ_WQBTgcX5';
const memories=[{id:'first-sight',date:'2025-06-21',title:'The First Sight',room:'Firsts',text:'The first chapter — before the story even knew it was becoming a story.'},{id:'first-talk',date:'2025-06-22',title:'The First Conversation',room:'Firsts',text:'From simply noticing each other to actually talking.'},{id:'annual-bus',date:'2025-11-05',title:'Annual Day Bus Ride',room:'School Days',text:'School buses, Annual Day practice, and little snapshots that became memories.'},{id:'annual-performance',date:'2025-11-08',title:'Annual Day',room:'School Days',text:'A school chapter around practice and the final performance.'},{id:'traditional-day',date:'2025-11-20',title:'Traditional Day',room:'Special Days',text:'A memorable school day preserved as one of the story’s little landmarks.'},{id:'pulao',date:'2026-02-14',title:'Pulao Lunch',room:'Special Days',text:'Purvi made pulao, and a normal lunch became a memory worth keeping.'}];
const app=document.querySelector('#app'),modal=document.querySelector('#modal');const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const date=d=>new Date(d+'T00:00:00').toLocaleDateString('en-IN',{day:'numeric',month:'long',year:'numeric'});
const api=async(path,opt={})=>{const r=await fetch(SUPABASE_URL+path,{...opt,headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json',...(opt.headers||{})}});if(!r.ok)throw Error(await r.text());return r.status===204?null:r.json()};
function shell(body){app.innerHTML=`<div class="world"><nav><button class="logo" data-page="home">♡ Purvi</button><div class="navlinks"><button data-page="story">Story</button><button data-page="museum">Museum</button><button data-page="wrapped">Chat Wrapped</button><button data-page="vault">🔐 Vault</button><button data-page="import">Import</button></div></nav>${body}</div>`;document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>page(b.dataset.page))}
function home(){app.innerHTML=`<div class="gift-page"><div class="stars">✦　·　✧　·　✦</div><div class="gift-card"><div class="ribbon">A little gift</div><div class="gift-icon">🎁</div><p class="eyebrow">FOR PURVI · 13 AUGUST</p><h1>Something made<br><i>just for you.</i></h1><p class="lead">It isn't just a birthday page. It's a little world made from the moments worth keeping.</p><button class="primary" id="openGift">Open your gift ✨</button></div><p class="tiny">A private memory world · made with care</p></div>`;document.querySelector('#openGift').onclick=()=>page('story')}
function story(){shell(`<main class="story"><section class="chapter intro"><span class="chapter-no">CHAPTER 01</span><p class="eyebrow">THE BEGINNING</p><h1>Every little story<br>has a first page.</h1><p>And ours starts much more quietly than you might expect.</p></section><section class="timeline">${memories.map((m,i)=>`<article class="memory-card" data-memory="${m.id}"><div class="memory-date">${date(m.date)}</div><div class="chapter-dot">${String(i+1).padStart(2,'0')}</div><div><span class="room">${esc(m.room)}</span><h2>${esc(m.title)}</h2><p>${esc(m.text)}</p><button class="text-btn">Open this memory →</button></div></article>`).join('')}</section><section class="chapter bridge"><p class="eyebrow">AND THE STORY KEPT GROWING</p><h2>Some moments are tiny.<br>They still become everything.</h2><button class="primary" data-page="museum">Walk into the Memory Museum →</button></section></main>`);document.querySelectorAll('[data-memory]').forEach(x=>x.onclick=()=>memory(x.dataset.memory))}
function museum(){shell(`<main class="museum"><section class="chapter"><p class="eyebrow">MEMORY MUSEUM</p><h1>Rooms full of little pieces.</h1><p>Step into a room and open whatever memory catches your eye.</p></section><div class="rooms">${['Firsts','School Days','Artist Purvi','Funny Moments','Emotional Moments','Special Days','Hidden Vault'].map((r,i)=>`<button class="room-card ${r==='Hidden Vault'?'locked':''}" data-room="${esc(r)}"><span>${['🌱','🏫','🎨','😂','🫂','✨','🔐'][i]}</span><small>ROOM ${String(i+1).padStart(2,'0')}</small><h2>${esc(r)}</h2><p>${r==='Hidden Vault'?'Something is waiting here.':'Open the collection →'}</p></button>`).join('')}</div><div id="roomContent" class="room-content"></div></main>`);document.querySelectorAll('[data-room]').forEach(b=>b.onclick=()=>{if(b.classList.contains('locked')){page('vault');return}const r=b.dataset.room;document.querySelector('#roomContent').innerHTML=`<h2>${esc(r)}</h2><div class="memory-grid">${memories.filter(m=>m.room===r).map(m=>`<article class="polaroid" data-memory="${m.id}"><span>${date(m.date)}</span><h3>${esc(m.title)}</h3><p>${esc(m.text)}</p></article>`).join('')||'<p class="soft">This room is waiting for more memories.</p>'}</div>`;document.querySelectorAll('#roomContent [data-memory]').forEach(x=>x.onclick=()=>memory(x.dataset.memory))})}
async function wrapped(){shell(`<main class="wrapped"><section class="chapter"><p class="eyebrow">CHAT WRAPPED</p><h1>The conversation<br>behind the story.</h1><p>Once the archive is imported, the numbers and moments come alive here.</p></section><div id="stats" class="stats"><div class="stat"><b>…</b><span>loading archive</span></div></div><section class="archive-search"><p class="eyebrow">SEARCH THE STORY</p><input id="chatSearch" placeholder="Search a word, phrase, nickname…"><div id="results"></div></section></main>`);try{const rows=await api('/rest/v1/chat_messages?select=sender,message_date&limit=10000');const c={},d=new Set;rows.forEach(x=>{c[x.sender]=(c[x.sender]||0)+1;d.add(x.message_date)});const top=Object.entries(c).sort((a,b)=>b[1]-a[1])[0];document.querySelector('#stats').innerHTML=`<div class="stat"><b>${rows.length.toLocaleString()}</b><span>messages loaded</span></div><div class="stat"><b>${d.size}</b><span>active dates</span></div><div class="stat"><b>${esc(top?.[0]||'—')}</b><span>top sender</span></div>`}catch(e){document.querySelector('#stats').innerHTML='<div class="stat"><b>♡</b><span>Import the archive to unlock this chapter.</span></div>'}document.querySelector('#chatSearch').oninput=e=>search(e.target.value)}
async function search(q){const box=document.querySelector('#results');if(q.trim().length<2){box.innerHTML='';return}box.innerHTML='<p class="soft">Searching…</p>';try{const r=await api(`/rest/v1/chat_messages?body=ilike.*${encodeURIComponent(q.trim())}*&select=message_date,message_time,sender,body&order=message_date.asc&limit=60`);box.innerHTML=r.map(x=>`<article class="chat-result"><small>${esc(x.message_date)} · ${esc(x.message_time)} · ${esc(x.sender)}</small><p>${esc(x.body)}</p></article>`).join('')||'<p class="soft">No matching moments.</p>'}catch(e){box.innerHTML='<p class="soft">Import the archive first.</p>'}}
function vault(){shell(`<main class="vault"><section class="vault-card"><div class="lock">🔐</div><p class="eyebrow">A HIDDEN PLACE</p><h1>The Secret Vault</h1><p>Some things should stay hidden until the right moment.</p><input id="code" placeholder="Enter the secret code" maxlength="24"><button class="primary" id="unlock">Unlock</button><p id="vaultMsg" class="soft"></p></section></main>`);document.querySelector('#unlock').onclick=()=>{const code=document.querySelector('#code').value.trim();document.querySelector('#vaultMsg').textContent=code?'The vault is still being prepared for the final reveal.':'Enter the secret code to continue.'}}
function birthday(){app.innerHTML=`<div class="birthday"><div class="confetti">✦　♡　✧　♡　✦</div><p class="eyebrow">13 · 08 · 2026</p><div class="cake">🎂</div><h1>Happy Birthday,<br><i>Purvi.</i></h1><p>This little world began with memories.<br>It ends with one more thing:</p><h2>there are so many more pages to write.</h2><button class="primary" data-page="home">Open the world again ♡</button></div>`;document.querySelector('[data-page]').onclick=()=>page('home')}
function memory(id){const m=memories.find(x=>x.id===id);modal.hidden=false;modal.innerHTML=`<div class="dialog"><button class="close" id="close">×</button><span class="room">${esc(m.room)}</span><p class="eyebrow">${date(m.date)}</p><h2>${esc(m.title)}</h2><p>${esc(m.text)}</p><div class="media-area"><strong>📸 Add to this memory</strong><small>Photos, videos and voice notes can be added directly from the website.</small><input id="mediaInput" type="file" accept="image/*,video/*,audio/*" multiple><button class="primary" id="upload">Upload to this memory</button><div id="mediaStatus"></div></div><div id="mediaList" class="media-list"></div></div>`;document.querySelector('#close').onclick=()=>modal.hidden=true;loadMedia(id);document.querySelector('#upload').onclick=()=>upload(id)}
async function signed(path){const r=await fetch(`${SUPABASE_URL}/storage/v1/object/sign/memory-media/${path}`,{method:'POST',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':'application/json'},body:JSON.stringify({expiresIn:3600})});if(!r.ok)throw Error(await r.text());const x=await r.json();return x.signedURL?.startsWith('http')?x.signedURL:`${SUPABASE_URL}/storage/v1${x.signedURL}`}
async function loadMedia(id){try{const rows=await api(`/rest/v1/memory_media?memory_id=eq.${encodeURIComponent(id)}&select=*&order=created_at.asc`);const urls=await Promise.all(rows.map(async x=>({...x,url:await signed(x.storage_path)})));document.querySelector('#mediaList').innerHTML=urls.map(x=>x.media_type==='image'?`<img src="${esc(x.url)}" alt="">`:x.media_type==='video'?`<video controls src="${esc(x.url)}"></video>`:`<audio controls src="${esc(x.url)}"></audio>`).join('')||'<p class="soft">No media yet.</p>'}catch(e){document.querySelector('#mediaList').innerHTML='<p class="soft">No media loaded yet.</p>'}}
async function upload(id){const input=document.querySelector('#mediaInput'),s=document.querySelector('#mediaStatus');if(!input.files.length){s.textContent='Choose media first.';return}s.textContent='Uploading…';try{for(const f of input.files){const path=`${id}/${crypto.randomUUID()}-${f.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;const r=await fetch(`${SUPABASE_URL}/storage/v1/object/memory-media/${path}`,{method:'POST',headers:{apikey:SUPABASE_KEY,Authorization:`Bearer ${SUPABASE_KEY}`,'Content-Type':f.type||'application/octet-stream'},body:f});if(!r.ok)throw Error(await r.text());await api('/rest/v1/memory_media',{method:'POST',body:JSON.stringify({memory_id:id,storage_path:path,media_type:f.type.startsWith('image/')?'image':f.type.startsWith('video/')?'video':'voice',file_name:f.name})})}s.textContent='Uploaded ♡';input.value='';loadMedia(id)}catch(e){s.textContent='Upload failed: '+e.message}}
function importPage(){shell(`<main class="import"><section class="chapter"><p class="eyebrow">CHAT ARCHIVE</p><h1>Bring the conversation home.</h1><p>Choose the original WhatsApp .txt. Everything is parsed on your device and sent in small batches.</p></section><section class="import-box"><input id="file" type="file" accept=".txt,text/plain"><button class="primary" id="go">Parse & import archive</button><p id="status" class="soft"></p><div id="progress"></div></section></main>`);document.querySelector('#go').onclick=importArchive}
function parseWhatsApp(t){const out=[];let cur=null;const re=/^(?:\[)?(\d{1,2})[\/.-](\d{1,2})[\/.-](\d{2,4}),?\s+(\d{1,2}:\d{2}(?::\d{2})?\s*(?:AM|PM)?)\]?(?:\s*[-–]\s*|\s+)([^:]+):\s?(.*)$/i;for(const line of t.replace(/\r/g,'').split('\n')){const m=line.match(re);if(m){if(cur)out.push(cur);let y=m[3].length===2?'20'+m[3]:m[3];cur={message_date:`${y}-${m[2].padStart(2,'0')}-${m[1].padStart(2,'0')}`,message_time:m[4],sender:m[5].trim(),body:m[6]||'',message_hash:''}}else if(cur&&line.trim())cur.body+='\n'+line}if(cur)out.push(cur);return out}
async function sha(s){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(s));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}
async function importArchive(){const f=document.querySelector('#file').files[0],s=document.querySelector('#status'),p=document.querySelector('#progress');if(!f){s.textContent='Choose the exported .txt first.';return}try{const rows=parseWhatsApp(await f.text());if(!rows.length)throw Error('No messages detected.');s.textContent=`Parsed ${rows.length.toLocaleString()} messages…`;for(const x of rows)x.message_hash=await sha(`${x.message_date}|${x.message_time}|${x.sender}|${x.body}`);let done=0,duplicates=0;for(let i=0;i<rows.length;i+=200){const b=rows.slice(i,i+200);const r=await api('/rest/v1/chat_messages?on_conflict=message_hash',{method:'POST',headers:{Prefer:'resolution=ignore-duplicates,return=representation'},body:JSON.stringify(b)});duplicates+=b.length-(r?.length||0);done+=b.length;p.textContent=`Processed ${done.toLocaleString()} / ${rows.length.toLocaleString()} · ${duplicates.toLocaleString()} duplicates skipped`;await new Promise(r=>setTimeout(r,0))}s.textContent=`Archive processed ♡ · ${duplicates.toLocaleString()} duplicates skipped.`}catch(e){s.textContent='Import failed: '+e.message}}
function page(p){({home,story,museum,wrapped,vault,birthday,import:importPage}[p]||home)()}modal.addEventListener('click',e=>{if(e.target===modal)modal.hidden=true});page('home');